#ifndef COMMANDS_H
#define COMMANDS_H

#include <vector>
#include <string>

namespace Commands {
    void cmdPwd(const std::vector<std::string>& commands);
    void cmdCd(const std::vector<std::string>& commands);
}

#endif
